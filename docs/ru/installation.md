# Руководство по настройке проекта

## 📋 Содержание

  - [Установка, Windows](#установка-windows)
    - [1. Настройка переменных окружения (.env)](#1-настройка-переменных-окружения-env)
    - [2. Настройка Google API](#2-настройка-google-api)
    - [3. Генерация JWT секрета](#3-генерация-jwt-секрета)
    - [4. Запуск приложения](#4-запуск-приложения)

---

## Установка, Windows

Чтобы запустить проект, сначала необходимо создать файл переменных окружения. Сделайте копию `.sample.env` и переименуйте её в `.env`.

### 1. Настройка переменных окружения (.env)

Ваш файл `.env` должен выглядеть примерно так:

```env
POSTGRES_DB=vanemarendaja_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD={Ваш пароль}

SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/vanemarendaja_db
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD={Ваш пароль}

GOOGLE_CLIENT_ID={Ваш Google Client ID}
GOOGLE_CLIENT_SECRET={Ваш Google Client Secret}

JWT_SECRET={Ваш сгенерированный JWT секрет}
```

---

### 2. Настройка Google API

Чтобы получить `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET`, следуйте этим инструкциям:

- **Получение ID:** [Google Developer Console](https://developers.google.com/identity/gsi/web/guides/get-google-api-clientid#get_your_google_api_client_id)
- **Платформа аутентификации:** При создании новой выберите **External**, а не Internal.
- **Настройка:** После создания Client ID, нажмите на него и заполните следующие поля:
  - **Authorized JavaScript origins:**
    - `http://localhost`
    - `http://localhost:3000` (или ваш порт, если он отличается)
  - **Authorized redirect URIs:**
    - `http://localhost:8080/login/oauth2/code/google`
- **Секрет:** `GOOGLE_CLIENT_SECRET` находится в том же месте, что и Client ID.

---

### 3. Генерация JWT секрета

Для `JWT_SECRET` вам понадобится Node.js. Выполните эту команду в терминале и скопируйте вывод в файл `.env`:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

---

### 4. Запуск приложения

1. **Backend & Database:** Когда `.env` готов, запустите Docker Engine (например, Docker Desktop). В корневом каталоге проекта выполните:

   ```bash
   docker compose up
   ```

2. **Frontend:** После запуска Docker, перейдите в каталог `frontend` и установите необходимые пакеты:

   ```bash
   npm i
   ```

3. **Запуск:** Запустите frontend приложение:
   ```bash
   npm run dev
   ```

Если всё прошло успешно, вы увидите сообщение в консоли:
`Local: http://localhost:3000`

Удерживайте **CTRL** и **левой кнопкой мыши** нажмите на эту ссылку, чтобы открыть frontend в браузере.
